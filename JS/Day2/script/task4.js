//calculate circle area
let circle = parseFloat(prompt("What is the value of circle radius?"));
let area = Math.PI * Math.pow(circle,2);
alert("Total area of circle is "+area);

//calculate square root
let num = parseInt(prompt("What is the value you want to calculate its square root?"));
let sqr = Math.sqrt(num);
alert("Square root of "+ num +" is"+sqr);

//calculate angle cos
let angle = parseFloat(prompt("What is the value of angle?"));
let cosain = Math.cos(angle * Math.PI / 180);
alert("cos of "+ angle +"ْ is"+cosain);